# Roadmap

These items are outside the initial implementation contract.

- Chrome or Chromium build target.
- URL-to-local-root mapping for local HTTP preview servers.
- Local path allow/deny rules.
- Editable remote snapshots with explicit export behavior.
- Packaged native host binary for users without a Python setup.
- Optional command to open the current Firefox page source in Neovim.
